# Optional: set backend base URL for production builds (leave empty when using Vite dev proxy)
# VITE_API_BASE_URL=http://xiaolongya.cn:8091

# Bearer token (without the 'Bearer ' prefix)
VITE_API_TOKEN=xiaolong
